function setVolume(cast,valor) {
   // var slider = document.getElementById('volume-'+cast);
   // slider.value = valor;
   // slider.style.background = 'linear-gradient(to right, var(--acento)' + slider.value + '%, var(--fondo2) ' + slider.value + '%)';
   // slider.oninput = function() {
   //    this.style.background = 'linear-gradient(to right, var(--acento)' + this.value + '%, var(--fondo2) ' + this.value + '%)';
   // };
}